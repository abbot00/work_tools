#include <stdio.h>
/**
 *main - this program, prints the inscription
 *Return: 0 if exited properly, non-zer 
int main(void)
{

char Mystr[] ="\"Programming is like building a multilingual puzzle";
puts(Mystr); 
return 0;
}
