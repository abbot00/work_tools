#include <stdio.h>
/**
 *main - this program prints a series of string usning printf
 *Return: 0 if exited properly. 
 */
int main(void)
{
printf("with proper grammer, but the outcome is a piece of art,");
return (0);
}
